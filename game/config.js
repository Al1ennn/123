// ==========================================
// ФАЙЛ: config.js (Скелетна Анімація)
// ==========================================

const GAME_CONFIG = {
    // 1. НАЛАШТУВАННЯ СКЕЛЕТА (Довжина кісток у пікселях)
    skeleton: {
        torso: { width: 34, height: 45 },
        arm: { width: 10, height: 25 },
        leg: {
            thighLength: 25, // Довжина стегна
            calfLength: 25,  // Довжина гомілки
            width: 12
        }
    },

    // 2. НАЛАШТУВАННЯ АНІМАЦІЇ БІГУ (Амплітуда в градусах)
    runAnimation: {
        speed: 0.25,        // Швидкість махання ногами
        legSwing: 35,       // Наскільки далеко відводиться стегно (градуси)
        kneeBend: 45        // Максимальний згин коліна (градуси)
    },

    // 3. ФІЗИКА ТА РУХ
    physics: {
        gravity: 1.5,          
        jumpPower: -20,        
        laneSmoothness: 0.1,   
        baseSpeed: 10,         
        acceleration: 0.00015  
    },

    // 4. ПАРАМЕТРИ ГРАВЦЯ (ХІТБОКСИ)
    player: {
        rollHeight: 50,        
        rollDuration: 40       
    }
};
